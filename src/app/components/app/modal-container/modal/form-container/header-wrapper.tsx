import styled from 'checkout/styled-components';

export const HeaderWrapper = styled.div`
    margin-bottom: 20px;
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    align-items: center;
    position: relative;
`;
